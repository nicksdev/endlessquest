

Rooms Setup:


name:
intro: Copy containing an intro for when the user enters the room
encounter: Copy and mob generation if there is combat in this room
combat: function to run through the combat
options: the options for actions that can be taken in a room






Call a room
Create the options object
Create the room object








var room0 = function() {
    return {
        roomName: "Roomname Here",
        roomintro: "Intro Here",
        roomOption: function() {
            genOptions()
        }
    }

};




    document.getElementById("userInput").removeEventListener("keyup", function (event) {
        event.preventDefault();
        if (event.keyCode == 13) {
            document.getElementById("consoleDiv").innerHTML += "STARTING A NEW ROUND<p>";
            rooms["combatTest"]["combat"]["combatRound"]();
        }
    } );